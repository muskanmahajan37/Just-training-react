import styles from './AddButton.module.css';

const AddButton = function () {
    return (
        <button className={styles.AdicionarButton}>
            Adicionar
        </button>
    )
}

export default AddButton